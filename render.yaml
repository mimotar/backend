services:
  - type: web
    name: usermanager
    env: node
    buildCommand: npm install && npm run prisma:migrate:deploy && npm run prisma:generate && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
